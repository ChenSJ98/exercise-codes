{
  "tasks": [
    {
      "type": "shell",
      "label": "g++ build active file",
      "command": "/opt/cuda/bin/g++",
      "args": [
        "-g",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}"
      ],
      "options": {
        "cwd": "/opt/cuda/bin"
      }
    }
  ],
  "version": "2.0.0"
}